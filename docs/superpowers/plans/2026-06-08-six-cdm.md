# Six-CDM — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Site de pronostics entre amis pour la Coupe du Monde 2026 : pronostics de score par match verrouillés au coup d'envoi, barème 1/3, bonus tournoi, classement, déployé sur GitHub Pages.

**Architecture:** SPA Vite/React/TS statique parlant directement à Supabase (Postgres + Auth Google + RLS). Le verrouillage des paris et le calcul des points sont garantis côté base (RLS + vues SQL). `matches.json` dans le repo est la source de vérité humaine des matchs/résultats, synchronisée vers Supabase.

**Tech Stack:** Vite, React, TypeScript, TanStack Router, TanStack Query, Zustand, shadcn/ui + Tailwind, Supabase, Vitest + Testing Library, GitHub Actions → GitHub Pages.

**Spec:** `docs/superpowers/specs/2026-06-08-six-cdm-design.md`

---

## Structure des fichiers

```
six-cdm/
├─ .github/workflows/deploy.yml      # CI build + déploiement Pages
├─ index.html
├─ vite.config.ts                    # base: '/six-cdm/'
├─ tsconfig.json / tsconfig.node.json
├─ tailwind.config.ts / postcss.config.js
├─ components.json                   # config shadcn
├─ .env.example                      # VITE_SUPABASE_URL / VITE_SUPABASE_ANON_KEY
├─ src/
│  ├─ main.tsx                       # bootstrap React + Router + QueryClient
│  ├─ routeTree / routes/            # TanStack Router (login, index, matchs, match.$id, classement, bonus, profil)
│  ├─ lib/
│  │  ├─ supabase.ts                 # client Supabase
│  │  ├─ scoring.ts                  # computePoints() pur (display + tests)
│  │  └─ utils.ts                    # cn() shadcn
│  ├─ stores/ui.ts                   # Zustand : filtres calendrier, thème
│  ├─ hooks/                         # TanStack Query : useMatches, usePredictions, useLeaderboard, useBonus, useAuth
│  ├─ components/                    # MatchCard, PredictionForm, Countdown, LeaderboardTable, ...
│  ├─ data/matches.json              # 104 matchs (source de vérité)
│  └─ types/db.ts                    # types générés Supabase
├─ supabase/migrations/              # SQL : schéma, RLS, vues, seed
└─ scripts/seed-matches.ts           # sync matches.json → Supabase (doc/manuel)
```

---

## Phase 0 — Scaffolding du projet

### Task 0.1 : Initialiser le projet Vite + React + TS

**Files:**
- Create: `package.json`, `vite.config.ts`, `tsconfig.json`, `tsconfig.node.json`, `index.html`, `src/main.tsx`, `src/App.tsx`, `.gitignore`, `.env.example`

- [ ] **Step 1: Scaffolder avec pnpm**

Le repo contient déjà `package.json` minimal (shadcn) et `docs/`. On scaffolde par-dessus sans écraser `docs/`.

Run:
```bash
pnpm create vite@latest . --template react-ts
```
Si l'outil refuse à cause de fichiers existants, créer dans un sous-dossier temporaire puis copier `src/`, `index.html`, `vite.config.ts`, `tsconfig*.json` à la racine.

- [ ] **Step 2: Installer les dépendances de base**

Run:
```bash
pnpm add @supabase/supabase-js @tanstack/react-router @tanstack/react-query zustand
pnpm add -D @types/node vitest @testing-library/react @testing-library/jest-dom jsdom @vitejs/plugin-react
```

- [ ] **Step 3: Configurer `vite.config.ts` avec base path et Vitest**

```ts
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import path from 'node:path'

export default defineConfig({
  base: '/six-cdm/',
  plugins: [react()],
  resolve: { alias: { '@': path.resolve(__dirname, './src') } },
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./src/test/setup.ts'],
  },
})
```

- [ ] **Step 4: Créer le setup de test**

Create `src/test/setup.ts`:
```ts
import '@testing-library/jest-dom/vitest'
```

- [ ] **Step 5: Ajouter les scripts dans package.json**

```json
"scripts": {
  "dev": "vite",
  "build": "tsc -b && vite build",
  "preview": "vite preview",
  "test": "vitest run",
  "test:watch": "vitest"
}
```

- [ ] **Step 6: Créer `.env.example`**

```
VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
```
Ajouter `.env` et `.env.local` à `.gitignore`.

- [ ] **Step 7: Vérifier le build**

Run: `pnpm build`
Expected: build réussi, dossier `dist/` créé.

- [ ] **Step 8: Commit**

```bash
git add -A && git commit -m "chore: scaffold Vite + React + TS avec base path /six-cdm/"
```

### Task 0.2 : Configurer Tailwind + shadcn/ui

**Files:**
- Create: `tailwind.config.ts`, `postcss.config.js`, `components.json`, `src/index.css`, `src/lib/utils.ts`

- [ ] **Step 1: Installer Tailwind**

Run:
```bash
pnpm add -D tailwindcss postcss autoprefixer tailwindcss-animate
pnpm dlx tailwindcss init -p
```

- [ ] **Step 2: Initialiser shadcn**

Run: `pnpm dlx shadcn@latest init`
Choisir : TypeScript, style par défaut, variables CSS oui, alias `@/components` et `@/lib/utils`.

- [ ] **Step 3: Vérifier `src/lib/utils.ts`**

Doit contenir le helper `cn` :
```ts
import { clsx, type ClassValue } from 'clsx'
import { twMerge } from 'tailwind-merge'
export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```

- [ ] **Step 4: Ajouter quelques composants shadcn de base**

Run: `pnpm dlx shadcn@latest add button card input dialog table badge avatar sonner tabs`

- [ ] **Step 5: Vérifier le build**

Run: `pnpm build`
Expected: succès.

- [ ] **Step 6: Commit**

```bash
git add -A && git commit -m "chore: configurer Tailwind + shadcn/ui"
```

---

## Phase 1 — Supabase : schéma, RLS, vues

> Toutes les opérations DB se font via les outils MCP Supabase (`apply_migration`, `execute_sql`, `get_advisors`). Si aucun projet n'existe, en créer un (`create_project` après `confirm_cost`). Récupérer URL + clé anon via `get_project_url` / `get_publishable_keys`.

### Task 1.1 : Créer le projet et les tables

**Files:**
- Create: `supabase/migrations/0001_schema.sql` (copie locale documentaire de la migration appliquée via MCP)

- [ ] **Step 1: Confirmer/créer le projet Supabase**

Via MCP : `list_projects`. Si aucun projet adapté, `get_cost` → `confirm_cost` → `create_project` (nom `six-cdm`, région proche). Noter le `project_id`.

- [ ] **Step 2: Appliquer la migration schéma**

Via MCP `apply_migration` (name: `schema`), SQL :
```sql
create table profiles (
  id uuid primary key references auth.users(id) on delete cascade,
  display_name text not null,
  avatar_url text,
  created_at timestamptz not null default now()
);

create table matches (
  id int primary key,
  stage text not null check (stage in
    ('group','round_of_32','round_of_16','quarter','semi','third_place','final')),
  group_name text,
  home_team text not null,
  away_team text not null,
  kickoff_at timestamptz not null,
  venue text not null,
  home_score int check (home_score >= 0),
  away_score int check (away_score >= 0),
  status text not null default 'scheduled'
    check (status in ('scheduled','live','finished'))
);

create table predictions (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references profiles(id) on delete cascade,
  match_id int not null references matches(id) on delete cascade,
  home_score int not null check (home_score >= 0),
  away_score int not null check (away_score >= 0),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  unique (user_id, match_id)
);

create table bonus_questions (
  id int primary key generated always as identity,
  key text unique not null,
  label text not null,
  type text not null check (type in ('team','player','text')),
  lock_at timestamptz not null,
  correct_answer text,
  points int not null
);

create table bonus_predictions (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references profiles(id) on delete cascade,
  bonus_question_id int not null references bonus_questions(id) on delete cascade,
  answer text not null,
  unique (user_id, bonus_question_id)
);
```

- [ ] **Step 3: Sauver une copie locale**

Copier le même SQL dans `supabase/migrations/0001_schema.sql` pour versionner dans le repo.

- [ ] **Step 4: Vérifier**

Via MCP `list_tables` : confirmer les 5 tables.

- [ ] **Step 5: Commit**

```bash
git add supabase/ && git commit -m "feat(db): schéma initial (profiles, matches, predictions, bonus)"
```

### Task 1.2 : Trigger de création de profil

**Files:**
- Create: `supabase/migrations/0002_profile_trigger.sql`

- [ ] **Step 1: Appliquer la migration trigger via MCP**

`apply_migration` (name: `profile_trigger`) :
```sql
create or replace function handle_new_user()
returns trigger
language plpgsql
security definer set search_path = ''
as $$
begin
  insert into public.profiles (id, display_name, avatar_url)
  values (
    new.id,
    coalesce(new.raw_user_meta_data->>'full_name', new.raw_user_meta_data->>'name', split_part(new.email,'@',1)),
    new.raw_user_meta_data->>'avatar_url'
  );
  return new;
end;
$$;

create trigger on_auth_user_created
  after insert on auth.users
  for each row execute function handle_new_user();
```

- [ ] **Step 2: Copie locale + commit**

```bash
git add supabase/migrations/0002_profile_trigger.sql
git commit -m "feat(db): trigger création de profil à l'inscription"
```

### Task 1.3 : Vues de scoring et classement

**Files:**
- Create: `supabase/migrations/0003_views.sql`

- [ ] **Step 1: Appliquer la migration des vues via MCP**

`apply_migration` (name: `views`) :
```sql
-- Points par pronostic : 3 si score exact, 1 si bon résultat, 0 sinon, NULL si non joué
create or replace view prediction_points as
select
  p.id, p.user_id, p.match_id,
  case
    when m.home_score is null or m.away_score is null then null
    when p.home_score = m.home_score and p.away_score = m.away_score then 3
    when sign(p.home_score - p.away_score) = sign(m.home_score - m.away_score) then 1
    else 0
  end as points
from predictions p
join matches m on m.id = p.match_id;

-- Points par bonus
create or replace view bonus_points as
select
  bp.id, bp.user_id, bp.bonus_question_id,
  case
    when bq.correct_answer is not null and bp.answer = bq.correct_answer then bq.points
    else 0
  end as points
from bonus_predictions bp
join bonus_questions bq on bq.id = bp.bonus_question_id;

-- Classement agrégé
create or replace view leaderboard as
select
  pr.id as user_id,
  pr.display_name,
  pr.avatar_url,
  coalesce(mp.match_points, 0) as match_points,
  coalesce(bn.bonus_points, 0) as bonus_points,
  coalesce(mp.match_points, 0) + coalesce(bn.bonus_points, 0) as total_points,
  coalesce(mp.exact_count, 0) as exact_count,
  coalesce(mp.result_count, 0) as result_count
from profiles pr
left join (
  select user_id,
    sum(points) as match_points,
    count(*) filter (where points = 3) as exact_count,
    count(*) filter (where points >= 1) as result_count
  from prediction_points where points is not null
  group by user_id
) mp on mp.user_id = pr.id
left join (
  select user_id, sum(points) as bonus_points
  from bonus_points group by user_id
) bn on bn.user_id = pr.id;
```

- [ ] **Step 2: Tester la vue avec des données fictives via MCP `execute_sql`**

Insérer un match joué + 2 pronos (un exact, un bon résultat), vérifier `prediction_points` renvoie 3 et 1. Puis nettoyer.
```sql
insert into matches (id,stage,home_team,away_team,kickoff_at,venue,home_score,away_score,status)
values (9001,'group','A','B', now() - interval '1 day','Test',2,1,'finished');
-- (insérer profils/pronos de test, sélectionner prediction_points, vérifier 3 et 1, puis delete)
```
Expected: prono (2-1) → 3 ; prono (1-0) → 1 ; prono (0-1) → 0.

- [ ] **Step 3: Nettoyer les données de test, copie locale, commit**

```bash
git add supabase/migrations/0003_views.sql
git commit -m "feat(db): vues prediction_points, bonus_points, leaderboard"
```

### Task 1.4 : Politiques RLS

**Files:**
- Create: `supabase/migrations/0004_rls.sql`

- [ ] **Step 1: Appliquer les policies via MCP**

`apply_migration` (name: `rls`) :
```sql
alter table profiles enable row level security;
alter table matches enable row level security;
alter table predictions enable row level security;
alter table bonus_questions enable row level security;
alter table bonus_predictions enable row level security;

-- profiles
create policy "profiles readable by authenticated"
  on profiles for select to authenticated using (true);
create policy "users manage own profile"
  on profiles for update to authenticated using (id = auth.uid());

-- matches : lecture seule côté client
create policy "matches readable by authenticated"
  on matches for select to authenticated using (true);

-- predictions : lecture ouverte, écriture verrouillée au coup d'envoi
create policy "predictions readable by authenticated"
  on predictions for select to authenticated using (true);
create policy "users insert own prediction before kickoff"
  on predictions for insert to authenticated
  with check (
    user_id = auth.uid()
    and exists (select 1 from matches m where m.id = match_id and m.kickoff_at > now())
  );
create policy "users update own prediction before kickoff"
  on predictions for update to authenticated
  using (user_id = auth.uid())
  with check (
    user_id = auth.uid()
    and exists (select 1 from matches m where m.id = match_id and m.kickoff_at > now())
  );
create policy "users delete own prediction before kickoff"
  on predictions for delete to authenticated
  using (
    user_id = auth.uid()
    and exists (select 1 from matches m where m.id = match_id and m.kickoff_at > now())
  );

-- bonus_questions : lecture seule
create policy "bonus_questions readable by authenticated"
  on bonus_questions for select to authenticated using (true);

-- bonus_predictions : lecture ouverte, écriture verrouillée à lock_at
create policy "bonus_predictions readable by authenticated"
  on bonus_predictions for select to authenticated using (true);
create policy "users insert own bonus before lock"
  on bonus_predictions for insert to authenticated
  with check (
    user_id = auth.uid()
    and exists (select 1 from bonus_questions q where q.id = bonus_question_id and q.lock_at > now())
  );
create policy "users update own bonus before lock"
  on bonus_predictions for update to authenticated
  using (user_id = auth.uid())
  with check (
    user_id = auth.uid()
    and exists (select 1 from bonus_questions q where q.id = bonus_question_id and q.lock_at > now())
  );
```

- [ ] **Step 2: Vérifier l'absence d'alertes de sécurité**

Via MCP `get_advisors` (type: security). Corriger toute alerte (ex : vue exposée). Pour les vues, ajouter `security_invoker = true` si recommandé :
```sql
alter view prediction_points set (security_invoker = true);
alter view bonus_points set (security_invoker = true);
alter view leaderboard set (security_invoker = true);
```

- [ ] **Step 3: Copie locale + commit**

```bash
git add supabase/migrations/0004_rls.sql
git commit -m "feat(db): RLS — verrou des paris au coup d'envoi, lecture ouverte"
```

---

## Phase 2 — Données des matchs

### Task 2.1 : Construire `matches.json` depuis le PDF FIFA

**Files:**
- Create: `src/data/matches.json`, `src/types/match.ts`

- [ ] **Step 1: Définir le type Match**

Create `src/types/match.ts`:
```ts
export type Stage =
  | 'group' | 'round_of_32' | 'round_of_16'
  | 'quarter' | 'semi' | 'third_place' | 'final'

export interface MatchSeed {
  id: number            // numéro de match FIFA (1..104)
  stage: Stage
  group_name: string | null
  home_team: string
  away_team: string
  kickoff_at: string    // ISO 8601 UTC
  venue: string
}
```

- [ ] **Step 2: Extraire les 104 matchs du PDF officiel**

Lire le PDF `https://digitalhub.fifa.com/asset/8c1dfbc0-9a88-4021-a291-616c920800b2/FWC26-Match-Schedule_French.pdf` (via l'outil Read sur le PDF ou WebFetch). Pour chaque match : numéro, phase, groupe, équipes, date/heure (convertir en UTC ISO), stade. Écrire le tableau dans `src/data/matches.json`.

Format d'un élément :
```json
{ "id": 1, "stage": "group", "group_name": "A", "home_team": "Mexico", "away_team": "TBD", "kickoff_at": "2026-06-11T19:00:00Z", "venue": "Estadio Azteca, Mexico City" }
```
Les équipes inconnues à la date du build (barrages) restent `"TBD"` et seront mises à jour comme les résultats.

- [ ] **Step 3: Valider le JSON**

Écrire un test `src/data/matches.test.ts` :
```ts
import { describe, it, expect } from 'vitest'
import matches from './matches.json'
import type { MatchSeed } from '@/types/match'

describe('matches.json', () => {
  const data = matches as MatchSeed[]
  it('contient 104 matchs', () => expect(data.length).toBe(104))
  it('a des ids uniques 1..104', () => {
    const ids = new Set(data.map(m => m.id))
    expect(ids.size).toBe(104)
  })
  it('a des kickoff_at ISO valides', () => {
    for (const m of data) expect(Number.isNaN(Date.parse(m.kickoff_at))).toBe(false)
  })
  it('a 12 groupes (A..L) en phase de groupes', () => {
    const groups = new Set(data.filter(m => m.stage === 'group').map(m => m.group_name))
    expect(groups.size).toBe(12)
  })
})
```

- [ ] **Step 4: Lancer les tests**

Run: `pnpm test src/data/matches.test.ts`
Expected: PASS (4 tests).

- [ ] **Step 5: Commit**

```bash
git add src/data/ src/types/match.ts
git commit -m "feat(data): calendrier des 104 matchs CDM 2026 (source PDF FIFA)"
```

### Task 2.2 : Seeder les matchs et bonus dans Supabase

**Files:**
- Create: `scripts/seed-matches.ts` (documentaire ; le seed effectif passe par MCP)

- [ ] **Step 1: Générer le SQL d'insertion depuis le JSON**

Construire les `insert into matches (...) values ...` à partir de `src/data/matches.json` (status `scheduled`, scores null). Appliquer via MCP `execute_sql` par lots.

- [ ] **Step 2: Seeder les questions bonus via MCP**

`execute_sql` :
```sql
insert into bonus_questions (key, label, type, lock_at, points) values
  ('winner','Vainqueur du tournoi','team', (select min(kickoff_at) from matches), 10),
  ('finalist','Finaliste perdant','team', (select min(kickoff_at) from matches), 5),
  ('top_scorer','Meilleur buteur (Soulier d''or)','player', (select min(kickoff_at) from matches), 8);
```

- [ ] **Step 3: Vérifier**

Via MCP `execute_sql` : `select count(*) from matches;` → 104 ; `select count(*) from bonus_questions;` → 3.

- [ ] **Step 4: Documenter le workflow de synchro dans `scripts/seed-matches.ts`**

Commentaire expliquant : « Source de vérité = src/data/matches.json. Pour mettre à jour matchs/résultats : éditer le JSON, puis appliquer via MCP execute_sql (update matches set home_score=, away_score=, status='finished' where id=). »

- [ ] **Step 5: Commit**

```bash
git add scripts/ && git commit -m "chore(db): seed matchs + questions bonus + doc synchro"
```

---

## Phase 3 — Couche données front

### Task 3.1 : Client Supabase et types

**Files:**
- Create: `src/lib/supabase.ts`, `src/types/db.ts`

- [ ] **Step 1: Générer les types TypeScript**

Via MCP `generate_typescript_types`, écrire le résultat dans `src/types/db.ts`.

- [ ] **Step 2: Créer le client**

Create `src/lib/supabase.ts`:
```ts
import { createClient } from '@supabase/supabase-js'
import type { Database } from '@/types/db'

const url = import.meta.env.VITE_SUPABASE_URL
const key = import.meta.env.VITE_SUPABASE_ANON_KEY
if (!url || !key) throw new Error('Variables Supabase manquantes (.env)')

export const supabase = createClient<Database>(url, key)
```

- [ ] **Step 3: Build**

Run: `pnpm build`
Expected: succès (vérifie que les types compilent).

- [ ] **Step 4: Commit**

```bash
git add src/lib/supabase.ts src/types/db.ts
git commit -m "feat(data): client Supabase + types générés"
```

### Task 3.2 : Fonction de scoring pure (TDD)

**Files:**
- Create: `src/lib/scoring.ts`, `src/lib/scoring.test.ts`

> Reflète exactement la règle de la vue SQL `prediction_points`. Utilisée pour l'affichage des points côté UI. Doit rester synchrone avec la vue.

- [ ] **Step 1: Écrire le test (FAIL)**

Create `src/lib/scoring.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { computePoints } from './scoring'

describe('computePoints', () => {
  it('score exact = 3', () => {
    expect(computePoints({h:2,a:1},{h:2,a:1})).toBe(3)
  })
  it('bon résultat (victoire dom) = 1', () => {
    expect(computePoints({h:2,a:1},{h:3,a:0})).toBe(1)
  })
  it('bon résultat (nul) = 1', () => {
    expect(computePoints({h:1,a:1},{h:0,a:0})).toBe(1)
  })
  it('mauvais résultat = 0', () => {
    expect(computePoints({h:2,a:1},{h:0,a:2})).toBe(0)
  })
  it('match non joué = null', () => {
    expect(computePoints({h:2,a:1}, null)).toBeNull()
  })
})
```

- [ ] **Step 2: Lancer (FAIL)**

Run: `pnpm test src/lib/scoring.test.ts`
Expected: FAIL (computePoints non défini).

- [ ] **Step 3: Implémenter**

Create `src/lib/scoring.ts`:
```ts
export interface Score { h: number; a: number }

export function computePoints(pred: Score, result: Score | null): number | null {
  if (!result) return null
  if (pred.h === result.h && pred.a === result.a) return 3
  if (Math.sign(pred.h - pred.a) === Math.sign(result.h - result.a)) return 1
  return 0
}
```

- [ ] **Step 4: Lancer (PASS)**

Run: `pnpm test src/lib/scoring.test.ts`
Expected: PASS (5 tests).

- [ ] **Step 5: Commit**

```bash
git add src/lib/scoring.ts src/lib/scoring.test.ts
git commit -m "feat: fonction de scoring 1/3 (TDD)"
```

### Task 3.3 : Store Zustand (UI) et utilitaire de verrou

**Files:**
- Create: `src/stores/ui.ts`, `src/lib/lock.ts`, `src/lib/lock.test.ts`

- [ ] **Step 1: Écrire le test du verrou (FAIL)**

Create `src/lib/lock.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { isLocked } from './lock'

describe('isLocked', () => {
  it('verrouillé si le coup d\'envoi est passé', () => {
    expect(isLocked('2020-01-01T00:00:00Z', new Date('2026-06-08'))).toBe(true)
  })
  it('ouvert si le coup d\'envoi est futur', () => {
    expect(isLocked('2030-01-01T00:00:00Z', new Date('2026-06-08'))).toBe(false)
  })
})
```

- [ ] **Step 2: Lancer (FAIL)**

Run: `pnpm test src/lib/lock.test.ts`
Expected: FAIL.

- [ ] **Step 3: Implémenter le verrou**

Create `src/lib/lock.ts`:
```ts
export function isLocked(kickoffIso: string, now: Date = new Date()): boolean {
  return new Date(kickoffIso).getTime() <= now.getTime()
}
```

- [ ] **Step 4: Lancer (PASS)**

Run: `pnpm test src/lib/lock.test.ts`
Expected: PASS.

- [ ] **Step 5: Créer le store UI**

Create `src/stores/ui.ts`:
```ts
import { create } from 'zustand'
import type { Stage } from '@/types/match'

interface UIState {
  stageFilter: Stage | 'all'
  groupFilter: string | 'all'
  setStageFilter: (s: Stage | 'all') => void
  setGroupFilter: (g: string | 'all') => void
}

export const useUIStore = create<UIState>((set) => ({
  stageFilter: 'all',
  groupFilter: 'all',
  setStageFilter: (stageFilter) => set({ stageFilter }),
  setGroupFilter: (groupFilter) => set({ groupFilter }),
}))
```

- [ ] **Step 6: Commit**

```bash
git add src/stores/ src/lib/lock.ts src/lib/lock.test.ts
git commit -m "feat: store UI Zustand + utilitaire de verrou (TDD)"
```

### Task 3.4 : Hooks d'authentification

**Files:**
- Create: `src/hooks/useAuth.ts`

> Prérequis Supabase Auth : activer le provider Google (console Supabase), configurer le client OAuth Google, et ajouter l'URL de redirection `https://ludodwt.github.io/six-cdm/`. Documenter ces étapes dans le README.

- [ ] **Step 1: Implémenter le hook de session**

Create `src/hooks/useAuth.ts`:
```ts
import { useEffect, useState } from 'react'
import type { Session } from '@supabase/supabase-js'
import { supabase } from '@/lib/supabase'

export function useAuth() {
  const [session, setSession] = useState<Session | null>(null)
  const [loading, setLoading] = useState(true)

  useEffect(() => {
    supabase.auth.getSession().then(({ data }) => {
      setSession(data.session)
      setLoading(false)
    })
    const { data: sub } = supabase.auth.onAuthStateChange((_e, s) => setSession(s))
    return () => sub.subscription.unsubscribe()
  }, [])

  const signInWithGoogle = () =>
    supabase.auth.signInWithOAuth({
      provider: 'google',
      options: { redirectTo: `${window.location.origin}/six-cdm/` },
    })
  const signOut = () => supabase.auth.signOut()

  return { session, loading, signInWithGoogle, signOut }
}
```

- [ ] **Step 2: Build**

Run: `pnpm build`
Expected: succès.

- [ ] **Step 3: Commit**

```bash
git add src/hooks/useAuth.ts
git commit -m "feat(auth): hook session + Google OAuth"
```

### Task 3.5 : Hooks de données (TanStack Query)

**Files:**
- Create: `src/hooks/useMatches.ts`, `src/hooks/usePredictions.ts`, `src/hooks/useLeaderboard.ts`, `src/hooks/useBonus.ts`

- [ ] **Step 1: useMatches**

Create `src/hooks/useMatches.ts`:
```ts
import { useQuery } from '@tanstack/react-query'
import { supabase } from '@/lib/supabase'

export function useMatches() {
  return useQuery({
    queryKey: ['matches'],
    queryFn: async () => {
      const { data, error } = await supabase
        .from('matches').select('*').order('kickoff_at')
      if (error) throw error
      return data
    },
  })
}
```

- [ ] **Step 2: usePredictions (query + mutation upsert)**

Create `src/hooks/usePredictions.ts`:
```ts
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'
import { supabase } from '@/lib/supabase'

export function usePredictions() {
  return useQuery({
    queryKey: ['predictions'],
    queryFn: async () => {
      const { data, error } = await supabase.from('predictions').select('*')
      if (error) throw error
      return data
    },
  })
}

export function useUpsertPrediction() {
  const qc = useQueryClient()
  return useMutation({
    mutationFn: async (p: { user_id: string; match_id: number; home_score: number; away_score: number }) => {
      const { error } = await supabase
        .from('predictions')
        .upsert({ ...p, updated_at: new Date().toISOString() }, { onConflict: 'user_id,match_id' })
      if (error) throw error
    },
    onSuccess: () => {
      qc.invalidateQueries({ queryKey: ['predictions'] })
      qc.invalidateQueries({ queryKey: ['leaderboard'] })
    },
  })
}
```

- [ ] **Step 3: useLeaderboard**

Create `src/hooks/useLeaderboard.ts`:
```ts
import { useQuery } from '@tanstack/react-query'
import { supabase } from '@/lib/supabase'

export function useLeaderboard() {
  return useQuery({
    queryKey: ['leaderboard'],
    queryFn: async () => {
      const { data, error } = await supabase
        .from('leaderboard').select('*').order('total_points', { ascending: false })
      if (error) throw error
      return data
    },
  })
}
```

- [ ] **Step 4: useBonus (questions + pronos + mutation)**

Create `src/hooks/useBonus.ts`:
```ts
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'
import { supabase } from '@/lib/supabase'

export function useBonusQuestions() {
  return useQuery({
    queryKey: ['bonus_questions'],
    queryFn: async () => {
      const { data, error } = await supabase.from('bonus_questions').select('*').order('id')
      if (error) throw error
      return data
    },
  })
}

export function useBonusPredictions() {
  return useQuery({
    queryKey: ['bonus_predictions'],
    queryFn: async () => {
      const { data, error } = await supabase.from('bonus_predictions').select('*')
      if (error) throw error
      return data
    },
  })
}

export function useUpsertBonus() {
  const qc = useQueryClient()
  return useMutation({
    mutationFn: async (p: { user_id: string; bonus_question_id: number; answer: string }) => {
      const { error } = await supabase
        .from('bonus_predictions').upsert(p, { onConflict: 'user_id,bonus_question_id' })
      if (error) throw error
    },
    onSuccess: () => {
      qc.invalidateQueries({ queryKey: ['bonus_predictions'] })
      qc.invalidateQueries({ queryKey: ['leaderboard'] })
    },
  })
}
```

- [ ] **Step 5: Build + commit**

Run: `pnpm build` (Expected: succès)
```bash
git add src/hooks/
git commit -m "feat(data): hooks TanStack Query (matches, predictions, leaderboard, bonus)"
```

---

## Phase 4 — Routing & coquille de l'app

### Task 4.1 : Bootstrap Router + QueryClient + garde d'auth

**Files:**
- Modify: `src/main.tsx`
- Create: `src/router.tsx`, `src/routes/__root.tsx`, `src/components/AppLayout.tsx`

- [ ] **Step 1: Configurer le QueryClient et le Router dans main.tsx**

Replace `src/main.tsx`:
```tsx
import React from 'react'
import ReactDOM from 'react-dom/client'
import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
import { RouterProvider } from '@tanstack/react-router'
import { router } from './router'
import './index.css'

const queryClient = new QueryClient()

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <QueryClientProvider client={queryClient}>
      <RouterProvider router={router} />
    </QueryClientProvider>
  </React.StrictMode>,
)
```

- [ ] **Step 2: Créer le router avec basepath**

Create `src/router.tsx` : déclarer les routes (login, index, matchs, match/$id, classement, bonus, profil) avec `createRouter({ routeTree, basepath: '/six-cdm' })`. Utiliser le code-based routing de TanStack Router (rootRoute + route enfants) pour éviter le plugin de génération de fichiers. Chaque route pointe vers son composant de page (Phase 5).

- [ ] **Step 3: Layout racine avec navigation**

Create `src/components/AppLayout.tsx` : barre de nav (Dashboard, Matchs, Classement, Bonus, Profil), affichage du profil connecté + bouton déconnexion, `<Outlet />`. Redirige vers `/login` si `useAuth().session` est null (hors route login).

- [ ] **Step 4: Build + commit**

Run: `pnpm build` (Expected: succès)
```bash
git add src/main.tsx src/router.tsx src/routes/ src/components/AppLayout.tsx
git commit -m "feat: routing TanStack + layout + garde d'authentification"
```

### Task 4.2 : Page Login

**Files:**
- Create: `src/routes/login.tsx`

- [ ] **Step 1: Implémenter la page**

Bouton « Se connecter avec Google » appelant `signInWithGoogle()`. Si déjà connecté, rediriger vers `/`. Design : carte centrée, branding festival/foot (placeholder à raffiner en Phase 6).

- [ ] **Step 2: Build + commit**

```bash
git add src/routes/login.tsx
git commit -m "feat(auth): page de connexion Google"
```

---

## Phase 5 — Composants & pages

### Task 5.1 : Composant Countdown (TDD sur le formatage)

**Files:**
- Create: `src/lib/format.ts`, `src/lib/format.test.ts`, `src/components/Countdown.tsx`

- [ ] **Step 1: Test du formatage de durée (FAIL)**

Create `src/lib/format.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { formatCountdown } from './format'

describe('formatCountdown', () => {
  it('affiche j/h/m', () => {
    const ms = ((2*24+3)*60 + 15) * 60 * 1000
    expect(formatCountdown(ms)).toBe('2j 3h 15m')
  })
  it('affiche "Coup d\'envoi" si <= 0', () => {
    expect(formatCountdown(-1)).toBe("Coup d'envoi")
  })
})
```

- [ ] **Step 2: Lancer (FAIL)**

Run: `pnpm test src/lib/format.test.ts`
Expected: FAIL.

- [ ] **Step 3: Implémenter**

Create `src/lib/format.ts`:
```ts
export function formatCountdown(ms: number): string {
  if (ms <= 0) return "Coup d'envoi"
  const totalMin = Math.floor(ms / 60000)
  const d = Math.floor(totalMin / (60 * 24))
  const h = Math.floor((totalMin % (60 * 24)) / 60)
  const m = totalMin % 60
  return `${d}j ${h}h ${m}m`
}
```

- [ ] **Step 4: Lancer (PASS)**

Run: `pnpm test src/lib/format.test.ts`
Expected: PASS.

- [ ] **Step 5: Composant Countdown**

Create `src/components/Countdown.tsx` : prend `kickoffIso`, met à jour chaque minute via `useEffect`+`setInterval`, affiche `formatCountdown`.

- [ ] **Step 6: Commit**

```bash
git add src/lib/format.ts src/lib/format.test.ts src/components/Countdown.tsx
git commit -m "feat: composant Countdown + formatage (TDD)"
```

### Task 5.2 : PredictionForm (composant + test de verrou)

**Files:**
- Create: `src/components/PredictionForm.tsx`, `src/components/PredictionForm.test.tsx`

- [ ] **Step 1: Test (FAIL) — désactivé après le coup d'envoi**

Create `src/components/PredictionForm.test.tsx`:
```tsx
import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import { PredictionForm } from './PredictionForm'

describe('PredictionForm', () => {
  it('désactive les champs si le match a commencé', () => {
    render(<PredictionForm kickoffIso="2020-01-01T00:00:00Z" homeTeam="A" awayTeam="B" onSubmit={() => {}} />)
    expect(screen.getByLabelText('Score A')).toBeDisabled()
  })
  it('active les champs si le match est à venir', () => {
    render(<PredictionForm kickoffIso="2030-01-01T00:00:00Z" homeTeam="A" awayTeam="B" onSubmit={() => {}} />)
    expect(screen.getByLabelText('Score A')).not.toBeDisabled()
  })
})
```

- [ ] **Step 2: Lancer (FAIL)**

Run: `pnpm test src/components/PredictionForm.test.tsx`
Expected: FAIL.

- [ ] **Step 3: Implémenter**

Create `src/components/PredictionForm.tsx` : deux `Input` numériques (`aria-label` "Score A"/"Score B" dérivés des noms d'équipe), bouton « Valider ». Désactivé via `isLocked(kickoffIso)`. Appelle `onSubmit({home,away})`. Valeurs initiales `defaultHome`/`defaultAway` optionnelles.

```tsx
import { useState } from 'react'
import { Input } from '@/components/ui/input'
import { Button } from '@/components/ui/button'
import { isLocked } from '@/lib/lock'

interface Props {
  kickoffIso: string
  homeTeam: string
  awayTeam: string
  defaultHome?: number
  defaultAway?: number
  onSubmit: (s: { home: number; away: number }) => void
}

export function PredictionForm({ kickoffIso, homeTeam, awayTeam, defaultHome, defaultAway, onSubmit }: Props) {
  const locked = isLocked(kickoffIso)
  const [home, setHome] = useState(defaultHome ?? 0)
  const [away, setAway] = useState(defaultAway ?? 0)
  return (
    <form onSubmit={(e) => { e.preventDefault(); onSubmit({ home, away }) }} className="flex items-center gap-2">
      <Input aria-label={`Score ${homeTeam}`} type="number" min={0} value={home}
        disabled={locked} onChange={(e) => setHome(+e.target.value)} className="w-16" />
      <span>-</span>
      <Input aria-label={`Score ${awayTeam}`} type="number" min={0} value={away}
        disabled={locked} onChange={(e) => setAway(+e.target.value)} className="w-16" />
      <Button type="submit" disabled={locked}>Valider</Button>
    </form>
  )
}
```
(Note : les `aria-label` du test "Score A"/"Score B" correspondent à `homeTeam="A"`/`awayTeam="B"`.)

- [ ] **Step 4: Lancer (PASS)**

Run: `pnpm test src/components/PredictionForm.test.tsx`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/components/PredictionForm.tsx src/components/PredictionForm.test.tsx
git commit -m "feat: PredictionForm avec verrou au coup d'envoi (TDD)"
```

### Task 5.3 : MatchCard

**Files:**
- Create: `src/components/MatchCard.tsx`

- [ ] **Step 1: Implémenter**

Carte affichant équipes, stade, `Countdown`, le `PredictionForm` (pré-rempli avec le prono existant du user), et si le match est terminé : le score réel + les points obtenus (`computePoints`). Reçoit le match, le prono du user, et `onSubmit`.

- [ ] **Step 2: Build + commit**

Run: `pnpm build`
```bash
git add src/components/MatchCard.tsx
git commit -m "feat: MatchCard (prono + countdown + résultat/points)"
```

### Task 5.4 : Page Matchs (calendrier filtrable)

**Files:**
- Create: `src/routes/matchs.tsx`

- [ ] **Step 1: Implémenter**

Liste les `useMatches()`, applique les filtres du store Zustand (phase, groupe via `Tabs`/`Select`), affiche une `MatchCard` par match. Mutation via `useUpsertPrediction()` avec `user_id = session.user.id`. Toast de confirmation (`sonner`). Gérer loading/erreur.

- [ ] **Step 2: Build + commit**

```bash
git add src/routes/matchs.tsx
git commit -m "feat: page Matchs (calendrier + pronos filtrables)"
```

### Task 5.5 : Page détail Match

**Files:**
- Create: `src/routes/match.$id.tsx`, `src/components/PredictionsTable.tsx`

- [ ] **Step 1: PredictionsTable**

`Table` shadcn listant tous les pronos (jointure `predictions` + `profiles` côté requête) avec pseudo, score pronostiqué, et points si match terminé.

- [ ] **Step 2: Page détail**

Affiche le match (via `useMatches` filtré ou requête dédiée par id), le `PredictionForm` du user, et la `PredictionsTable` (pronos visibles par tous).

- [ ] **Step 3: Build + commit**

```bash
git add src/routes/match.\$id.tsx src/components/PredictionsTable.tsx
git commit -m "feat: page détail match + table des pronos"
```

### Task 5.6 : Page Classement

**Files:**
- Create: `src/routes/classement.tsx`, `src/components/LeaderboardTable.tsx`

- [ ] **Step 1: LeaderboardTable**

`Table` triée par `total_points` : rang, avatar+pseudo, total, points matchs, points bonus, nb scores exacts. Met en évidence la ligne du user courant.

- [ ] **Step 2: Page**

Branche `useLeaderboard()`, gère loading/erreur, rend `LeaderboardTable`.

- [ ] **Step 3: Build + commit**

```bash
git add src/routes/classement.tsx src/components/LeaderboardTable.tsx
git commit -m "feat: page Classement (ladder)"
```

### Task 5.7 : Page Bonus

**Files:**
- Create: `src/routes/bonus.tsx`

- [ ] **Step 1: Implémenter**

Liste `useBonusQuestions()`. Pour chaque question : champ de réponse (texte/équipe/joueur), désactivé via `isLocked(question.lock_at)`. Pré-rempli avec `useBonusPredictions()`. Mutation `useUpsertBonus()`. Toast.

- [ ] **Step 2: Build + commit**

```bash
git add src/routes/bonus.tsx
git commit -m "feat: page Bonus tournoi"
```

### Task 5.8 : Page Profil + Dashboard

**Files:**
- Create: `src/routes/profil.tsx`, `src/routes/index.tsx`

- [ ] **Step 1: Profil**

Affiche pseudo + avatar (depuis `profiles`), édition du `display_name` (update Supabase), récap des points du user (depuis `leaderboard`).

- [ ] **Step 2: Dashboard (index)**

Prochains matchs non verrouillés sans prono (rappel), rang du user, raccourcis vers Matchs/Classement/Bonus.

- [ ] **Step 3: Build + commit**

```bash
git add src/routes/profil.tsx src/routes/index.tsx
git commit -m "feat: pages Profil + Dashboard"
```

---

## Phase 6 — Direction artistique (frontend-design)

### Task 6.1 : Appliquer la DA « festival + foot »

**Files:**
- Modify: `src/index.css` (thème, variables couleurs), composants existants

> **REQUIRED SUB-SKILL:** Utiliser `frontend-design:frontend-design` (+ MCP shadcn) pour cette tâche. Explorer une direction visuelle distinctive avant de coder.

- [ ] **Step 1: Définir le thème**

Palette chaude/festive (accents USA/Mexique/Canada), typographie expressive, tokens Tailwind/shadcn (variables CSS dans `src/index.css`). Mode sombre optionnel.

- [ ] **Step 2: Raffiner les composants clés**

MatchCard (dynamique, drapeaux/équipes mises en valeur), Countdown (urgence visuelle), LeaderboardTable (podium top 3), micro-animations de validation de prono (toast + transition).

- [ ] **Step 3: Vérifier visuellement**

Run: `pnpm dev` et inspecter chaque page. Capturer via chrome-devtools MCP si besoin.

- [ ] **Step 4: Build + commit**

```bash
git add -A && git commit -m "feat(ui): direction artistique festival + foot"
```

---

## Phase 7 — Déploiement GitHub Pages

### Task 7.1 : Fallback SPA + workflow CI

**Files:**
- Create: `.github/workflows/deploy.yml`, `public/404.html` (ou copie au build)
- Modify: `package.json` (script build copie 404)

- [ ] **Step 1: Fallback 404 SPA**

Ajouter au build la copie de `dist/index.html` → `dist/404.html` (script post-build) pour que les routes profondes fonctionnent sur Pages.
Modifier le script build :
```json
"build": "tsc -b && vite build && cp dist/index.html dist/404.html"
```

- [ ] **Step 2: Workflow GitHub Actions**

Create `.github/workflows/deploy.yml`:
```yaml
name: Deploy
on:
  push:
    branches: [main]
permissions:
  contents: read
  pages: write
  id-token: write
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with: { version: 9 }
      - uses: actions/setup-node@v4
        with: { node-version: 22, cache: pnpm }
      - run: pnpm install --frozen-lockfile
      - run: pnpm build
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
      - uses: actions/upload-pages-artifact@v3
        with: { path: dist }
  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
```

- [ ] **Step 3: Documenter dans README**

Create/append `README.md` : configurer les secrets GitHub (`VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`), activer Pages (source: GitHub Actions), configurer Google OAuth dans Supabase + redirect `https://ludodwt.github.io/six-cdm/`, workflow de synchro des résultats (éditer `matches.json` → MCP `execute_sql`).

- [ ] **Step 4: Connecter le remote et pousser**

> Action sortante — confirmer avec l'utilisateur avant de pousser.
```bash
git remote add origin git@github.com:LudoDWT/six-cdm.git
git push -u origin main
```

- [ ] **Step 5: Commit**

```bash
git add .github/ README.md package.json
git commit -m "ci: déploiement GitHub Pages + fallback SPA"
```

---

## Phase 8 — Vérification de bout en bout

### Task 8.1 : Tests et parcours complet

- [ ] **Step 1: Toute la suite de tests**

Run: `pnpm test`
Expected: tous les tests PASS (matches.json, scoring, lock, format, PredictionForm).

- [ ] **Step 2: Build de production**

Run: `pnpm build`
Expected: succès, `dist/404.html` présent.

- [ ] **Step 3: Parcours manuel (`pnpm dev`)**

Vérifier : connexion Google → profil créé ; pronostiquer un match futur → enregistré ; un match passé est verrouillé (champs désactivés ET refus côté RLS si forcé) ; saisir un résultat via MCP → classement mis à jour ; pronos visibles par tous sur la page détail ; bonus enregistrés.

- [ ] **Step 4: Advisors Supabase**

Via MCP `get_advisors` (security + performance) : aucune alerte critique.

- [ ] **Step 5: Commit final / tag v1**

```bash
git commit --allow-empty -m "chore: v1 six-cdm prête"
```

---

## Notes de cohérence

- Le barème 1/3 vit à deux endroits : vue SQL `prediction_points` (source de vérité du classement) et `computePoints` (affichage). **Modifier les deux ensemble.**
- Le verrou repose sur `now()` Postgres (RLS) ET `isLocked` (UI). L'UI est cosmétique ; la base est la garantie.
- Équipes `TBD` (barrages) : traitées comme du texte ; mises à jour via le même workflow que les résultats.
- Variables `VITE_*` publiques : OK côté client car protégées par RLS.
```
